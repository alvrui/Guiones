# 📦 Dependencias para el backend (FastAPI + SQLite + Mistral API)

# FastAPI y ASGI
fastapi==0.109.0
uvicorn[standard]==0.27.0

# Base de datos
sqlalchemy>=2.0.36
aiosqlite==0.19.0  # Para SQLite asíncrono (opcional, si se usa async)

# Validación y serialización
pydantic==2.8.2
pydantic-settings==2.4.0

# Mistral API
mistralai==0.3.0

# Configuración y utilidades
python-dotenv==1.0.0
pyyaml==6.0.1

# Tipado (opcional, para desarrollo)
mypy==1.8.0

# Testing (opcional)
pytest==7.4.4
httpx==0.26.0

# Linting (opcional)
ruff==0.1.14
black==24.1.1
